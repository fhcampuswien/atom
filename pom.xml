<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>at.ac.fhcampuswien.atom</groupId>
	<artifactId>atom</artifactId>
	<version>1.0-SNAPSHOT</version>
	<packaging>pom</packaging>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

		<mavenVersion>3.0</mavenVersion>
		<atom.version>1.0-SNAPSHOT</atom.version>
		<java.version>1.8</java.version>

		<!-- <gwt.version>[2.5.1,)</gwt.version> -->
		<!-- <gwt.version>2.5.1</gwt.version>
		<gwt-mvn.version>2.5.1</gwt-mvn.version> -->

		<!-- cant switch to gwt 2.6. yet, because gwt-i18n-server is incompatible -->

		<!-- <gwt.version>2.6.1</gwt.version> -->
		
		<!-- <gwt.version>2.7.2.20150203-1500ori</gwt.version> -->
		<!-- <gwt.mavenplugin.version>2.7.0</gwt.mavenplugin.version> -->
		
		<!-- INCOMPATIBILTIES: -->
		<!-- gwt-ent reflection - only works with 2.6.1 and lower -->
		<!-- gwt-ent reflection - only works with old dev-mode, no super-dev mode
		        [ERROR] The Cross-Site-Iframe linker does not support <script> tags in the gwt.xml files, but the gwt.xml file  
		        (or the gwt.xml files which it includes) contains the following script tags: [INFO] gwtent/pagebus.js -->
		<!-- sencha gxt - only works with 2.7.0, not with current trunk (or xapi gwt fork) -->
		
		<gwt.version>2.6.1</gwt.version>
		<gwt.mavenplugin.version>2.6.1</gwt.mavenplugin.version>
		
		<!-- 2015-01-13: 2.7.0 = wont compile, errors from gwt-ent & sencha gxt! -->
		<!-- <gwt.version>2.7.0</gwt.version> -->

		<hibernate.version>4.3.1.Final</hibernate.version>
	</properties>

	<modules>
		<module>atom-core</module>
		<module>atom-domain</module>
		<module>atom-client</module>
		<module>atom-server</module>
	</modules>
	
	<build>
		<pluginManagement>
			<plugins>
				<plugin> <!-- globally setting java version here -->
					<artifactId>maven-compiler-plugin</artifactId>
					<version>3.2</version>
					<configuration>
						<source>${java.version}</source>
						<target>${java.version}</target>
					</configuration>
				</plugin>
				
				<plugin>
					<!-- this plugin is used in atom-core and atom-domain to provide
						 their source-code to the gwt java-to-javascript compiler -->
					<artifactId>maven-source-plugin</artifactId>
					<version>2.2</version>
					<executions>
						<execution>
							<id>attach-sources</id>
							<phase>package</phase>
							<goals>
								<goal>jar-no-fork</goal>
							</goals>
						</execution>
					</executions>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>
</project>